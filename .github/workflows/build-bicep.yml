name: Build Bicep template

on:
  push:
    branches:
      - master
    paths:
      - 'deploy/azure/main.bicep'
      - 'deploy/azure/modules/**.bicep'
      - 'deploy/azure-demo-resources/main.bicep'
      - 'deploy/azure-demo-resources/modules/**.bicep'
      - '.github/workflows/build-bicep.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-commit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      # ubuntu-latest ships Azure CLI already; this only installs the bicep
      # extension for it (or upgrades it if it's somehow already present).
      - name: Install/upgrade Bicep CLI
        run: az bicep install || az bicep upgrade

      - name: Build deploy/azure/main.json from main.bicep
        run: az bicep build --file deploy/azure/main.bicep --outfile deploy/azure/main.json

      - name: Build deploy/azure-demo-resources/main.json from main.bicep
        run: az bicep build --file deploy/azure-demo-resources/main.bicep --outfile deploy/azure-demo-resources/main.json

      - name: Commit regenerated main.json files if they changed
        run: |
          if git diff --quiet -- deploy/azure/main.json deploy/azure-demo-resources/main.json; then
            echo "main.json files already up to date - nothing to commit."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add deploy/azure/main.json deploy/azure-demo-resources/main.json
          git commit -m "chore: regenerate main.json from main.bicep [skip ci]"
          git push
